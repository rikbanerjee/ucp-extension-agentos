@AGENTS.md

<!-- This file is a pure import of AGENTS.md, so every rule there — including the focused
     challenge chrome on `/webmcp-showcase` and `/agent-ready-storefront` (RetailAgentOS identity,
     challenge badge, Run Demo, How It Works, Developer Evidence, GitHub, Back to RetailAgentOS;
     no dropdown-heavy company navigation, no large multi-column footer; other routes keep the
     normal platform navigation; never both systems on one route) — applies here verbatim. Add
     rules to AGENTS.md, not to this file, so there is exactly one home per fact. -->
