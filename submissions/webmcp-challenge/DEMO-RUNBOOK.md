# Judge and recording runbook

Open `/webmcp-showcase` on the public HTTPS origin. `/agent-ready-storefront` is compatibility-only.

1. Confirm three initial native planning tools register through `document.modelContext` when the browser supports WebMCP.
2. Use the canonical Fresh Corner weekend-breakfast prompt shown on the page. Search named Farm Eggs and Artisan Sourdough, evaluate the Farm Eggs plan with $30/local delivery, and confirm `STOCK_STALE` / `REPAIRABLE`.
3. Confirm repair tools appear and cart preparation is withheld. Invoke `apply_plan_repair`, approve Cage-Free Eggs, then confirm `prepare_validated_cart` is available before the repair result returns.
4. Invoke cart preparation: confirm $15.99, local delivery, and no checkout/order/payment. Optionally revise bread to two loaves: $24.49 of $30, $5.51 remaining.
5. Switch to TheCustomHub. Evaluate 25 black robotics-team shirts (6 Youth Small, 11 Youth Medium, 8 Youth Large), then submit the structured configuration, $500 budget ceiling, and delivery request within 15 days. Confirm `QUOTE_REQUIRED`, `fixedPrice: null`, `deliveryPromise: null`, and no cart/order/payment/checkout.

Guided replay is “Guided replay · Same RetailAgentOS handlers · No external agent.” It is a truthful fallback, not evidence of native invocation. Do not claim deployed verification unless this build has actually been deployed and tested.
