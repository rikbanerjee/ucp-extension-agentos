# Judging map

Start at the **WebMCP Live Demo** — the RetailAgentOS WebMCP Agent Storefront at `/webmcp-showcase`. Its focused header reaches every row below in one click: Run Demo (`#webmcp-mission`), How It Works (`#why-webmcp`), Developer Evidence (`#developer-evidence`).

| What to verify | Where to see it |
| --- | --- |
| Three initial native planning tools | `/webmcp-showcase` Mission Control and Developer Evidence |
| Named Farm Eggs stale-inventory refusal | Fresh Corner's $30 breakfast mission |
| Human approval and dynamic safe action | Approval card, then registered `prepare_validated_cart` |
| $15.99 cart / no checkout | Cart result and withheld capability list |
| $24.49 optional revision / $5.51 remaining | Optional cart revision panel |
| Quote-only commerce | TheCustomHub `QUOTE_REQUIRED` and `fixedPrice: null` |
| Native/replay parity and attribution | Mission Control and raw Developer Evidence |
| Business case in one screen | "Why the storefront becomes meaningfully better with WebMCP" (`#why-webmcp`) |

Fresh Corner and TheCustomHub are controlled fixtures. There is no live TheCustomHub backend or generalized remote/server MCP claim.
