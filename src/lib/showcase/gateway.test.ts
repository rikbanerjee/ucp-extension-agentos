import { describe, expect, it } from 'vitest';
import { createShowcaseGateway } from './fixture';
describe('showcase gateway', () => { const gateway = createShowcaseGateway(1760000000000); it('uses the engine decision for region blocks', () => { const result = gateway.evaluateOffer('v_b_001_1', 1, { marketRegion: 'GB' }); expect(result.code).toBe('REGION_RESTRICTED'); expect(result.eligible).toBe(false); }); it('does not mutate blocked carts and is idempotent', () => { const input = [{ productId: 'v_b_001_1', quantity: 1 }]; const first = gateway.prepareCart(input, 'idempotent-1', { marketRegion: 'GB' }); const second = gateway.prepareCart(input, 'idempotent-1', { marketRegion: 'US' }); expect(first.cart).toBeNull(); expect(second).toEqual(first); }); it('does not fabricate custom-order price', () => { expect(gateway.requestQuote({ productId: 'v_customhub_quote_001', quantity: 25, requirements: 'Robotics team shirts', idempotencyKey: 'quote-key-1' }).fixedPrice).toBeNull(); }); });
